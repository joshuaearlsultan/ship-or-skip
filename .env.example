# Copy this file to .env.local before running npm run dev.
# .env.local is gitignored and never committed.

# Required — get your key at console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Mock mode — set to "false" to call Anthropic; any other value (or absent) uses mock results
USE_MOCK_EVALUATIONS=true
